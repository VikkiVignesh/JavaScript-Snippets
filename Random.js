var rand=Math.random(); //using rand method
rand=rand*6;  // here 6 is the range, we can give any number till where we want
//above code will generate the number is decimal formate to ,get int form we should use floor()
rand=Math.floor(rand)
console.log(rand)